const LOGIN = 'LOGIN'
const LOGOUT = 'LOGOUT'
const UPDATE = 'UPDATE'
const START_SEEDING = 'START_SEEDING'
const FINISHED_SEEDING = 'FINISH_SEEDING'
const MESSAGE = 'MESSAGE'
const ELO_INITIAL = 1200

module.exports = {
  ELO_INITIAL,
  LOGIN,
  LOGOUT,
  MESSAGE,
  UPDATE,
  START_SEEDING,
  FINISHED_SEEDING
}
